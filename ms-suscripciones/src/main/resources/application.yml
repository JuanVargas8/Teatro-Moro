server:
  port: 9004

spring:
  application:
    name: ms-suscripciones

  datasource:
    url: jdbc:postgresql://postgres:5432/suscripciones
    username: postgres
    password: Caraluna.351

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka